<x-layouts.dashboard-layout title="{{ __('Compare candidates') }}" heading="{{ __('Compare candidates') }}" description="{{ __('Review selected candidates side by side before making a decision.') }}">
    <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-6"><p class="text-sm text-slate-400">{{ __('Select candidates from the ranking view to compare skills, match scores, and key gaps.') }}</p></div>
</x-layouts.dashboard-layout>
